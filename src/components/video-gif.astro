---
/**
 * See https://web.dev/patterns/web-vitals-patterns/video/video-gif
 */

interface Props {
  folder: string;
  name: string;
}

const { folder, name } = Astro.props;
---

<video autoplay loop muted playsinline class="rounded-lg bg-sweater-9">
  <source src={`/${folder}/${name}.mp4`} type="video/mp4" />
</video>
